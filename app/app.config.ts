export default defineAppConfig({
  ui: {
    colors: {
      primary: 'sangria',
      neutral: 'zinc'
    },
    input: {
      slots: {
        root: 'w-full'
      }
    },
    inputTags: {
      slots: {
        root: 'w-full'
      }
    },
    textarea: {
      slots: {
        root: 'w-full'
      }
    }
  }
})
